name: appbar_dropdown
description: AppBar Dropdown is an attractive UI element that allows for navigation via a dropdown menu in the AppBar.
version: 0.0.3
homepage: https://github.com/konectech/appbar_dropdown

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  pointer_interceptor: 0.9.0 # The latest (0.9.3) version doesn't work :( 


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0


flutter:
